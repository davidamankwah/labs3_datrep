import React from 'react'; //import react

export class Footer extends React.Component{ //inherits from react.component 
    render(){
      // display the following message ”My Footer in another component” 
        return (
            <div>
              <h1>My Footer in another component</h1>
            </div>
          );
    }
}